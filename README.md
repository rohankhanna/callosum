# codex-proxy

Local OpenAI-compatible HTTP endpoint that routes requests to a pool of upstream backends with health-aware selection.

Status: early development. Not ready for use.

See `docs/implementation-plan.md` for the current plan.

## Verify

```
make verify
```

Runs lint, format check, type check, and tests.
